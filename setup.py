from setuptools import setup

setup(
    name='mathtools',
    version='0.1.1',
    description='A library for doing discrete math in Python',
    packages=['mathtools'],
    python_requires='~=3.5'
)
