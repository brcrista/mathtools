from setuptools import setup

setup(
    name='mathtools',
    version='0.2.0',
    description='A library for doing discrete math in Python',
    packages=['mathtools'],
    python_requires='~=3.5'
)
